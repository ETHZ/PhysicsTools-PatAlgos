process TestEHR = {
    source = PoolSource { untracked vstring fileNames = { 'file:eh.root' } }
    untracked PSet maxEvents =  { untracked int32 input = -1 }

    module read = TestEventHypothesisReader {
        InputTag events = sols
    }    

    path p = {  read }
    
}
